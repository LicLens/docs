name: Mirror to readme.com repo

on:
  push:
    branches:
      - main

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout docs (source)
        uses: actions/checkout@v4
        with:
          path: docs
          fetch-depth: 0

      - name: Checkout readme (destination)
        uses: actions/checkout@v4
        with:
          repository: LicLens/readme
          ref: v1.0
          path: readme
          fetch-depth: 0                    # avoids shallow-push failures on divergence
          ssh-key: ${{ secrets.DEPLOY_KEY }}

      # ── DELTA ────────────────────────────────────────────────────────────────
      # Work out what actually changed in this push so downstream steps only
      # reprocess the affected files and their folders — not the whole tree.
      #   CHANGED_FILES   : added/modified docs (paths relative to docs/, NUL-safe list)
      #   DELETED_FILES   : docs removed in this push (relative to docs/)
      #   CHANGED_DIRS    : Documentation-relative folders touched (for _order/landing)
      #   SUMMARY_CHANGED : 'true' if SUMMARY.md changed -> regenerate ALL _order.yaml
      #   FULL_SYNC       : 'true' on first run / non-diffable push -> process everything
      # Lists are written to files under $RUNNER_TEMP to stay NUL-safe and avoid
      # env-size limits; flags go to $GITHUB_ENV.
      - name: Compute changed/deleted docs (delta)
        run: |
          cd docs
          BEFORE="${{ github.event.before }}"
          AFTER="${{ github.sha }}"
          TMP="$RUNNER_TEMP"
          : > "$TMP/changed.txt"
          : > "$TMP/deleted.txt"

          full_sync() {
            echo "FULL_SYNC=true"   >> "$GITHUB_ENV"
            echo "SUMMARY_CHANGED=true" >> "$GITHUB_ENV"
            # List every tracked doc (excludes are applied at sync time).
            git ls-files -z > "$TMP/changed.txt"
            echo "Delta: FULL SYNC (no usable previous SHA)"
          }

          # github.event.before is all-zeros on first push to a branch / forced cases.
          if [ -z "$BEFORE" ] || [ "$BEFORE" = "0000000000000000000000000000000000000000" ] \
             || ! git cat-file -e "$BEFORE^{commit}" 2>/dev/null; then
            full_sync
          else
            # Added (A), Copied (C), Modified (M), Renamed (R) -> need (re)processing.
            # Deleted (D) -> remove from mirror. Use -z for NUL-safe paths.
            git diff --name-only --diff-filter=ACMR -z "$BEFORE" "$AFTER" > "$TMP/changed.txt"
            git diff --name-only --diff-filter=D  -z "$BEFORE" "$AFTER" > "$TMP/deleted.txt"

            if [ ! -s "$TMP/changed.txt" ] && [ ! -s "$TMP/deleted.txt" ]; then
              echo "FULL_SYNC=false" >> "$GITHUB_ENV"
              echo "SUMMARY_CHANGED=false" >> "$GITHUB_ENV"
              echo "Delta: no doc changes detected"
            else
              echo "FULL_SYNC=false" >> "$GITHUB_ENV"
              if git diff --name-only "$BEFORE" "$AFTER" | grep -qx 'SUMMARY.md'; then
                echo "SUMMARY_CHANGED=true" >> "$GITHUB_ENV"
              else
                echo "SUMMARY_CHANGED=false" >> "$GITHUB_ENV"
              fi
            fi
          fi

          echo "CHANGED_LIST=$TMP/changed.txt" >> "$GITHUB_ENV"
          echo "DELETED_LIST=$TMP/deleted.txt" >> "$GITHUB_ENV"

          echo "=== changed (added/modified/renamed) ==="
          tr '\0' '\n' < "$TMP/changed.txt" || true
          echo "=== deleted ==="
          tr '\0' '\n' < "$TMP/deleted.txt" || true

      - name: Sync changed docs → readme/docs/Documentation (delta)
        run: |
          mkdir -p readme/docs/Documentation
          DEST="readme/docs/Documentation"

          if [ "${FULL_SYNC}" = "true" ]; then
            # First run / non-diffable: full mirror. Trailing slash = copy CONTENTS.
            # Note: NO --delete here; deletions are handled explicitly below so a
            # full re-run never nukes generated index/_order files mid-pipeline.
            rsync -av \
              --exclude='.git' \
              --exclude='.github' \
              --exclude='.DS_Store' \
              --exclude='SUMMARY.md' \
              docs/ "$DEST/"
          else
            # Copy only changed/added files, preserving their relative paths.
            # rsync --files-from reads a NUL-separated list; --from0 enables that.
            # We feed it the changed list with SUMMARY.md / VCS noise filtered out.
            tr '\0' '\n' < "$CHANGED_LIST" \
              | grep -v -e '^SUMMARY.md$' -e '^\.git' -e '\.DS_Store$' \
              | tr '\n' '\0' > "$RUNNER_TEMP/changed.sync"
            if [ -s "$RUNNER_TEMP/changed.sync" ]; then
              rsync -av --from0 --files-from="$RUNNER_TEMP/changed.sync" \
                docs/ "$DEST/"
            else
              echo "No content files to copy (only deletions / SUMMARY change)."
            fi
          fi

      # ── DELTA: deletions ─────────────────────────────────────────────────────
      # Remove from the mirror anything deleted in the source. A GitBook README.md
      # was relocated to index.md in the mirror, so map deleted README.md -> index.md.
      # Done against the git index so the removal is committed at the end.
      - name: Apply deletions to mirror (delta)
        if: ${{ env.FULL_SYNC != 'true' }}
        run: |
          DEST="readme/docs/Documentation"
          if [ ! -s "$DELETED_LIST" ]; then
            echo "No deletions to apply."
            exit 0
          fi
          cd readme
          # Read NUL-separated deleted paths; translate and remove from index+tree.
          while IFS= read -r -d '' rel; do
            case "$rel" in
              SUMMARY.md|.git*|*.DS_Store) continue ;;
            esac
            # README.md -> index.md mapping (root README went to getting-started/).
            mapped="$rel"
            case "$rel" in
              README.md)            mapped="getting-started/index.md" ;;
              */README.md)          mapped="${rel%README.md}index.md" ;;
            esac
            target="docs/Documentation/$mapped"
            if [ -e "$target" ]; then
              git rm -f --quiet --ignore-unmatch "$target" || rm -f "$target"
              echo "  deleted: $mapped"
            else
              echo "  (already absent): $mapped"
            fi
          done < "../$DELETED_LIST"

      - name: Relocate .gitbook/assets → assets and rewrite references
        run: |
          cd readme/docs/Documentation
          if [ -d ".gitbook/assets" ]; then
            mkdir -p assets
            cp -a .gitbook/assets/. assets/
            rm -rf .gitbook
          fi
          # Rewrite every .gitbook/assets variant in markdown:
          # .gitbook/assets, ./.gitbook/assets, and any ../ depth.
          # Delta: only rewrite files that changed this run (full tree on FULL_SYNC).
          if [ "${FULL_SYNC}" = "true" ]; then
            find . -type f -name '*.md' -print0
          else
            tr '\0' '\n' < "$CHANGED_LIST" | grep -e '\.md$' \
              | sed 's#^#./#' | tr '\n' '\0'
          fi | xargs -0 --no-run-if-empty sed -i -E \
            -e 's#(\.\./)*\.gitbook/assets#assets#g' \
            -e 's#\./\.gitbook/assets#assets#g' \
            -e 's#\.gitbook/assets#assets#g'


      - name: Relocate root README into getting-started as landing page (index)
        run: |
          python3 - <<'EOF'
          import os, re

          root = "readme/docs/Documentation"
          src  = os.path.join(root, "README.md")
          if not os.path.exists(src):
              print("No root README.md to relocate")
              raise SystemExit(0)

          text = open(src, encoding="utf-8").read()

          # README moves one level deeper (Documentation/ -> Documentation/getting-started/),
          # so re-relativize its internal links to resolve from the new location.
          def fix(m):
              pre, target, post = m.group(1), m.group(2), m.group(3)
              if re.match(r'^[a-z][a-z0-9+.\-]*:', target, re.I) or target.startswith('#'):
                  return m.group(0)
              path, sep, anchor = target.partition('#')
              if not path:
                  return m.group(0)
              newpath = os.path.relpath(os.path.normpath(path), "getting-started").replace(os.sep, "/")
              return f'{pre}{newpath}{sep}{anchor}{post}'
          text = re.sub(r'(\]\()([^)\s]+)((?:\s+"[^"]*")?\))', fix, text)

          # The root README BECOMES the getting-started landing page itself
          # (getting-started/index.md), so /docs/getting-started shows this content
          # instead of an empty stub. No separate "welcome" child page is created.
          dest_dir = os.path.join(root, "getting-started")
          os.makedirs(dest_dir, exist_ok=True)
          open(os.path.join(dest_dir, "index.md"), "w", encoding="utf-8").write(text)
          os.remove(src)
          print("Relocated README.md -> getting-started/index.md (landing page, links re-relativized)")
          EOF

      # ── FIX ─────────────────────────────────────────────────────────────────
      # ReadMe requires:
      #   (a) every folder to have an index.md  (folder landing page), and
      #   (b) NO two pages sharing a slug.
      # GitBook ships one README.md per folder, all of which slugify to "readme"
      # and collide -> 404 / broken rendering. We convert each remaining
      # README.md -> index.md and rewrite the links that pointed at it. The root
      # README is already handled above, so only NESTED folder READMEs remain here.
      - name: Convert nested folder README.md → index.md (folder landing pages)
        run: |
          python3 - <<'EOF'
          import os, re, glob

          root = "readme/docs/Documentation"

          # 1) Find every nested README.md (root one is already gone -> welcome.md).
          readmes = [p for p in glob.glob(os.path.join(root, '**', 'README.md'), recursive=True)]

          renamed = []
          for src in readmes:
              dst = os.path.join(os.path.dirname(src), "index.md")
              # Never clobber an index.md the author already provided.
              if os.path.exists(dst):
                  print(f"SKIP (index.md already exists): {os.path.relpath(dst, root)}")
                  continue
              os.rename(src, dst)
              renamed.append(os.path.relpath(src, root).replace(os.sep, "/"))

          # 2) Rewrite links across ALL pages that pointed at a renamed README,
          #    e.g. [..](foo/README.md) or [..](./README.md) -> .../index.md.
          #    Match README(.md) as a path component; preserve #anchors + titles.
          link_re = re.compile(r'(\]\()([^)\s]+)((?:\s+"[^"]*")?\))')
          def rewrite(text):
              def fix(m):
                  pre, target, post = m.group(1), m.group(2), m.group(3)
                  if re.match(r'^[a-z][a-z0-9+.\-]*:', target, re.I) or target.startswith('#'):
                      return m.group(0)
                  path, sep, anchor = target.partition('#')
                  # README or README.md as the final path component
                  new = re.sub(r'(^|/)README(\.md)?$', lambda mm: f'{mm.group(1)}index{mm.group(2) or ""}', path)
                  if new == path:
                      return m.group(0)
                  return f'{pre}{new}{sep}{anchor}{post}'
              return link_re.sub(fix, text)

          changed = 0
          for path in glob.glob(os.path.join(root, '**', '*.md'), recursive=True):
              raw = open(path, encoding="utf-8").read()
              new = rewrite(raw)
              if new != raw:
                  open(path, "w", encoding="utf-8").write(new)
                  changed += 1

          print(f"Renamed {len(renamed)} README.md -> index.md: {renamed}")
          print(f"Rewrote README links in {changed} page(s)")
          EOF

      # ── FIX (Option B) ───────────────────────────────────────────────────────
      # When a category is clicked, show its FIRST article's content — WITHOUT
      # removing that article as its own sidebar entry. For every folder that has
      # no index.md yet, we COPY the first child's body into a new index.md and
      # give that index a DISTINCT, folder-derived title (e.g. "Understanding the
      # Basics"). The first article stays in place under its own title.
      #
      # Distinct titles matter: a category index and its first article sharing a
      # title/slug is exactly what caused the original 404s, so the copy is
      # re-titled, never a verbatim duplicate of the child's frontmatter.
      #
      # Runs AFTER README->index (author/README landing pages win) and BEFORE the
      # macro/frontmatter/_order steps (so the new index is processed normally).
      - name: Fill empty category landing (index.md) from first article (content copy)
        run: |
          python3 - <<'EOF'
          import os, re
          from collections import OrderedDict

          summary_path = "docs/SUMMARY.md"
          root         = "readme/docs/Documentation"

          def humanize(s):
              return re.sub(r'[-_]', ' ', s).strip().title() or "Overview"

          def strip_frontmatter(raw):
              # Return body only (drop any leading --- ... --- block); the category
              # index gets its OWN frontmatter/title injected downstream.
              if raw.lstrip().startswith('---'):
                  m = re.match(r'^\s*---\s*\n.*?\n---\s*\n?(.*)$', raw, re.DOTALL)
                  if m:
                      return m.group(1)
              return raw

          # First DIRECT child page per folder, in SUMMARY.md order.
          entry_re = re.compile(r'^(\s*)[\*\-]\s+\[.*?\]\((.*?)\)')
          first_child = OrderedDict()   # folder rel-path -> first direct child basename (no .md)

          if os.path.exists(summary_path):
              for line in open(summary_path, encoding="utf-8"):
                  m = entry_re.match(line)
                  if not m:
                      continue
                  rel = m.group(2).strip().lstrip("./")
                  if not rel.endswith(".md"):
                      continue
                  rel = re.sub(r'(^|/)README\.md$', lambda mm: f'{mm.group(1)}index.md', rel)
                  parts = rel.split("/")
                  if len(parts) < 2:
                      continue                      # root-level page, not in a folder
                  folder = "/".join(parts[:-1])
                  name   = parts[-1][:-3]
                  if name == "index":
                      continue
                  if folder not in first_child:
                      first_child[folder] = name

          filled = []
          for folder, child in first_child.items():
              fdir = os.path.join(root, folder)
              if not os.path.isdir(fdir):
                  continue
              if os.path.exists(os.path.join(fdir, "index.md")):
                  continue                          # already has a landing page
              src = os.path.join(fdir, child + ".md")
              if not os.path.exists(src):
                  continue
              body = strip_frontmatter(open(src, encoding="utf-8").read()).lstrip("\n")
              # Give the category index a distinct, folder-derived title so it does
              # NOT collide with the first article's slug. excerpt/summary left for
              # the frontmatter step to derive from the copied body.
              title = humanize(os.path.basename(folder))
              index = (f"---\ntitle: {title}\nhidden: false\n---\n\n{body}")
              open(os.path.join(fdir, "index.md"), "w", encoding="utf-8").write(index)
              filled.append((folder, child))

          for folder, child in filled:
              print(f"  category landing {folder}/index.md <- copied from {child}.md (child kept)")
          print(f"Category landing pages filled from first article in {len(filled)} folder(s)")
          EOF

      - name: Convert GitBook content macros → ReadMe MDX (Callouts, etc.)
        run: |
          python3 - <<'EOF'
          import re, glob, os

          root  = "readme/docs/Documentation"

          # Delta: process only changed .md files (full tree on FULL_SYNC). The
          # changed list is source-relative; map to mirror paths (README->index).
          def target_md_files():
              if os.environ.get("FULL_SYNC") == "true":
                  return glob.glob(os.path.join(root, '**', '*.md'), recursive=True)
              listing = os.environ.get("CHANGED_LIST")
              out = []
              if listing and os.path.exists(listing):
                  data = open(listing, "rb").read().split(b"\0")
                  for raw in data:
                      rel = raw.decode("utf-8", "ignore").strip()
                      if not rel.endswith(".md") or rel == "SUMMARY.md":
                          continue
                      rel = re.sub(r'(^|/)README\.md$',
                                   lambda mm: f'{mm.group(1)}index.md', rel)
                      if rel == "index.md":          # root README -> getting-started
                          rel = "getting-started/index.md"
                      p = os.path.join(root, rel)
                      if os.path.exists(p):
                          out.append(p)
              return out

          # GitBook hint style -> (ReadMe Callout theme, Font Awesome icon). No emoji.
          THEME = {
              "info":    ("info",    "fa-circle-info"),
              "note":    ("default", "fa-circle-info"),
              "tip":     ("okay",    "fa-circle-check"),
              "success": ("okay",    "fa-circle-check"),
              "warning": ("warn",    "fa-triangle-exclamation"),
              "danger":  ("error",   "fa-circle-exclamation"),
          }

          def convert(text):
              # {% hint style="X" %} ... {% endhint %} -> ReadMe <Callout> (MDX, FA icon)
              def hint(m):
                  style = (m.group(1) or "info").lower()
                  theme, icon = THEME.get(style, ("default", "fa-circle-info"))
                  body = m.group(2).strip("\n")
                  # blank lines around body so MDX parses it as markdown blocks
                  return f'<Callout icon="{icon}" theme="{theme}">\n\n{body}\n\n</Callout>'
              text = re.sub(r'\{%\s*hint\s+style="([^"]+)"\s*%\}(.*?)\{%\s*endhint\s*%\}',
                            hint, text, flags=re.DOTALL)
              # {% code ... %} ```...``` {% endcode %} -> keep fenced code only
              text = re.sub(r'\{%\s*code[^%]*%\}\s*\n(.*?)\n\{%\s*endcode\s*%\}',
                            lambda m: m.group(1), text, flags=re.DOTALL)
              # {% content-ref url="X" %} [..](..) {% endcontent-ref %} -> plain md link
              text = re.sub(r'\{%\s*content-ref[^%]*%\}\s*(.*?)\s*\{%\s*endcontent-ref\s*%\}',
                            lambda m: m.group(1).strip(), text, flags=re.DOTALL)
              # {% embed url="X" %} -> bare URL (ReadMe auto-embeds)
              text = re.sub(r'\{%\s*embed\s+url="([^"]+)"\s*%\}', r'\1', text)
              # strip remaining structural GitBook tags so nothing renders literally
              text = re.sub(r'\{%\s*/?\s*(tabs|tab|endtab|endtabs|columns|column|'
                            r'stepper|step|endstep|endstepper)[^%]*%\}', '', text)
              # remove .md extension from internal links (ReadMe links by slug);
              # keep #anchors, link titles, external URLs and non-.md assets intact
              def strip_md(m):
                  pre, target, post = m.group(1), m.group(2), m.group(3)
                  if re.match(r'^[a-z][a-z0-9+.\-]*:', target, re.I) or target.startswith('#'):
                      return m.group(0)
                  path, sep, anchor = target.partition('#')
                  if path.endswith('.md'):
                      path = path[:-3]
                  return f'{pre}{path}{sep}{anchor}{post}'
              text = re.sub(r'(\]\()([^)\s]+)((?:\s+"[^"]*")?\))', strip_md, text)
              return text

          changed = 0
          for path in target_md_files():
              raw = open(path, encoding="utf-8").read()
              new = convert(raw)
              if new != raw:
                  open(path, "w", encoding="utf-8").write(new)
                  changed += 1
          print(f"GitBook macros converted in {changed} page(s)")
          EOF

      # ── FIX ─────────────────────────────────────────────────────────────────
      # Inject/merge frontmatter AND guarantee a UNIQUE title per page. ReadMe
      # derives slugs from titles, so two index.md files both titled (or auto-
      # named) the same will collide. We make folder index pages inherit a title
      # from SUMMARY.md / the folder name, and de-duplicate any clashes by
      # appending the parent folder context (Yurii's "README for Security and
      # Compliance" pattern, done automatically).
      - name: Inject / merge ReadMe frontmatter (unique title / summary / hidden)
        run: |
          python3 - <<'EOF'
          import os, re, glob, yaml

          summary_path  = "docs/SUMMARY.md"
          root          = "readme/docs/Documentation"
          # ReadMe's File Format schema (Documentation Structure page) names the
          # subtitle field `excerpt`. ReadMe's Bi-Directional Sync page calls it
          # `summary`. The docs conflict, so we write `excerpt` (schema) AND mirror
          # the same value to `summary` — identical value, can't render wrong either way.
          # Once you confirm which one your site uses, set MIRROR_SUMMARY = False.
          EXCERPT_FIELD  = "excerpt"
          MIRROR_SUMMARY = True
          # GitBook-only frontmatter keys ReadMe doesn't consume — drop to keep it clean.
          STRIP_KEYS    = {"cover", "coverY", "coverYAdjust", "icon", "layout"}

          # SUMMARY.md links point at README.md / *.md; map BOTH the original path
          # and its index.md-renamed equivalent to the human title in the link text.
          title_map = {}
          if os.path.exists(summary_path):
              link_re = re.compile(r'\[([^\]]+)\]\(([^)]+\.md)\)')
              for line in open(summary_path, encoding="utf-8"):
                  m = link_re.search(line)
                  if m:
                      rel = m.group(2).strip().lstrip("./")
                      title = m.group(1).strip()
                      title_map[rel] = title
                      # README.md was renamed to index.md in an earlier step
                      title_map[re.sub(r'(^|/)README\.md$',
                                       lambda mm: f'{mm.group(1)}index.md', rel)] = title

          def split_frontmatter(raw):
              if raw.lstrip().startswith('---'):
                  m = re.match(r'^\s*---\s*\n(.*?)\n---\s*\n?(.*)$', raw, re.DOTALL)
                  if m:
                      try:
                          data = yaml.safe_load(m.group(1)) or {}
                      except yaml.YAMLError:
                          data = {}
                      if isinstance(data, dict):
                          return data, m.group(2)
              return {}, raw

          def first_h1(text):
              m = re.search(r'^\s*#\s+(.+?)\s*$', text, re.MULTILINE)
              return m.group(1).strip() if m else None

          def first_paragraph(text):
              for block in re.split(r'\n\s*\n', text):
                  b = block.strip()
                  if not b or b.startswith('#') or b.startswith('---'):
                      continue
                  b = re.sub(r'[*_`>#]', '', b).replace('\n', ' ').strip()
                  return (b[:147] + '...') if len(b) > 150 else b
              return ""

          def humanize(s):
              return re.sub(r'[-_]', ' ', s).strip().title()

          def folder_title(rel):
              # For an index page at <folder>/index.md, derive a title from the
              # FOLDER name, not the literal word "index".
              d = os.path.dirname(rel)
              if d:
                  return humanize(os.path.basename(d))
              return "Overview"

          # Delta: which mirror-relative pages changed this run? We still READ the
          # whole tree (title de-dup must be global to prevent slug collisions —
          # the original 404 cause), but only WRITE pages that changed, plus any
          # page whose title we had to alter to resolve a collision.
          FULL = os.environ.get("FULL_SYNC") == "true"
          changed_rel = set()
          if not FULL:
              listing = os.environ.get("CHANGED_LIST")
              if listing and os.path.exists(listing):
                  for raw in open(listing, "rb").read().split(b"\0"):
                      rel = raw.decode("utf-8", "ignore").strip()
                      if not rel.endswith(".md") or rel == "SUMMARY.md":
                          continue
                      rel = re.sub(r'(^|/)README\.md$',
                                   lambda mm: f'{mm.group(1)}index.md', rel)
                      if rel == "index.md":
                          rel = "getting-started/index.md"
                      changed_rel.add(rel)
              # Index/landing pages created this run (first-article copy, stubs) won't
              # be in git's changed list yet — include any index.md whose folder was
              # touched so freshly-built landings get frontmatter too.
              touched_dirs = {os.path.dirname(r) for r in changed_rel}
              for d in list(touched_dirs):
                  idx = (d + "/index.md") if d else "index.md"
                  changed_rel.add(idx)

          # First pass: decide each page's title (read EVERY page).
          pages = []   # (path, rel, fm, body, title, is_index)
          for path in glob.glob(os.path.join(root, '**', '*.md'), recursive=True):
              raw      = open(path, encoding="utf-8").read()
              fm, body = split_frontmatter(raw)
              rel      = os.path.relpath(path, root).replace(os.sep, '/')
              is_index = os.path.basename(rel) == "index.md"

              if is_index:
                  # index pages: prefer explicit fm title, else SUMMARY title for the
                  # folder, else the folder name. Avoid first_h1 alone (often generic).
                  title = (fm.get("title")
                           or title_map.get(rel)
                           or folder_title(rel)
                           or first_h1(body)
                           or "Overview")
              else:
                  title = (fm.get("title")
                           or title_map.get(rel)
                           or first_h1(body)
                           or humanize(os.path.splitext(os.path.basename(rel))[0]))
              pages.append([path, rel, fm, body, title, is_index])

          # Second pass: de-duplicate titles (ReadMe slug collisions). When two
          # pages resolve to the same title, qualify with their parent folder.
          # Track which pages had their title changed by de-dup so we still write
          # them even if their file content was otherwise untouched this run.
          dedup_changed = set()
          seen = {}
          for entry in pages:
              t = entry[4]
              seen.setdefault(t.lower(), []).append(entry)
          for t_lower, group in seen.items():
              if len(group) <= 1:
                  continue
              for entry in group:
                  rel = entry[1]
                  parent = os.path.basename(os.path.dirname(rel))
                  if parent:
                      entry[4] = f"{entry[4]} ({humanize(parent)})"
                      dedup_changed.add(rel)

          # Final safety net: if any collision survives (e.g. same title, same
          # parent name in different trees), append a numeric suffix.
          final_seen = {}
          for entry in pages:
              key = entry[4].lower()
              if key in final_seen:
                  final_seen[key] += 1
                  entry[4] = f"{entry[4]} {final_seen[key]}"
                  dedup_changed.add(entry[1])
              else:
                  final_seen[key] = 1

          changed = total = skipped = 0
          for path, rel, fm, body, title, is_index in pages:
              total += 1
              # Delta gate: only rebuild pages that changed this run OR whose title
              # we had to alter for collision-safety. Everything else is left as-is.
              if not FULL and rel not in changed_rel and rel not in dedup_changed:
                  skipped += 1
                  continue

              new = {}
              new["title"]       = title
              sub                = fm.get("excerpt") or fm.get("summary") or first_paragraph(body)
              new[EXCERPT_FIELD] = sub
              if MIRROR_SUMMARY:
                  new["summary"] = sub
              new["hidden"]      = fm.get("hidden", False)

              # SEO description lives under metadata.description per ReadMe's schema.
              metadata = dict(fm.get("metadata") or {})
              if fm.get("description") and "description" not in metadata:
                  metadata["description"] = fm["description"]
              if metadata:
                  new["metadata"] = metadata

              for k, v in fm.items():
                  if k in STRIP_KEYS or k in ("title", "excerpt", "summary",
                                              "hidden", "description", "metadata"):
                      continue
                  new[k] = v

              out = ("---\n"
                     + yaml.dump(new, default_flow_style=False, allow_unicode=True,
                                 sort_keys=False, width=4096)
                     + "---\n\n" + body.lstrip("\n"))
              raw = open(path, encoding="utf-8").read()
              if out != raw:
                  open(path, "w", encoding="utf-8").write(out)
                  changed += 1

          print(f"Frontmatter normalized: {changed} written, {skipped} skipped (delta), "
                f"of {total} pages (field='{EXCERPT_FIELD}', mirror_summary={MIRROR_SUMMARY})")
          EOF

      # ── FIX ─────────────────────────────────────────────────────────────────
      # Build a landing page for every folder, then write _order.yaml for every
      # folder — in ONE step so stubbing and ordering stay consistent. Fixes:
      #   * subfolder-only folders (e.g. `msp`) that had no index.md and vanished
      #   * top-level folders (e.g. `reports`) missing from the root _order.yaml
      # SUMMARY.md drives ORDER; the on-disk tree drives COMPLETENESS (any folder
      # present on disk is included even if SUMMARY missed it).
      - name: Ensure landing pages and generate _order.yaml for every folder
        run: |
          python3 - <<'EOF'
          import re, os, yaml
          from collections import OrderedDict

          summary_path = "docs/SUMMARY.md"
          root         = "readme/docs/Documentation"

          def humanize(s):
              return re.sub(r'[-_]', ' ', s).strip().title() or "Overview"

          def has_markdown_below(dirpath):
              for _, _, files in os.walk(dirpath):
                  if any(f.endswith(".md") for f in files):
                      return True
              return False

          # Delta scope: which folders' _order.yaml may need rewriting. If SUMMARY.md
          # changed (or FULL_SYNC), rewrite ALL; otherwise only folders touched this
          # run, plus their parent chain (a new page can change a parent's child list)
          # and root. Stubbing + completeness still run tree-wide (cheap dir walks),
          # but _order.yaml WRITES are scoped to keep the commit tight.
          FULL = os.environ.get("FULL_SYNC") == "true"
          SUMMARY_CHANGED = os.environ.get("SUMMARY_CHANGED") == "true"
          order_scope = None   # None => all folders
          if not FULL and not SUMMARY_CHANGED:
              order_scope = {""}   # always include root
              def add_chain(rel):
                  rel = re.sub(r'(^|/)README\.md$', lambda mm: f'{mm.group(1)}index.md', rel)
                  d = os.path.dirname(rel)
                  while True:
                      order_scope.add(d)
                      if not d:
                          break
                      d = os.path.dirname(d)
              for env_list in (os.environ.get("CHANGED_LIST"), os.environ.get("DELETED_LIST")):
                  if env_list and os.path.exists(env_list):
                      for raw in open(env_list, "rb").read().split(b"\0"):
                          r = raw.decode("utf-8", "ignore").strip()
                          if r.endswith(".md") and r != "SUMMARY.md":
                              add_chain(r)

          # 1) STUB: ensure every doc folder has an index.md (covers subfolder-only
          #    folders like `msp` and any folder with no README/SUMMARY entry).
          stubbed = 0
          for dirpath, dirnames, filenames in os.walk(root):
              if os.path.basename(dirpath) == "assets":
                  dirnames[:] = []           # don't descend into assets
                  continue
              if "index.md" in filenames:
                  continue
              if not has_markdown_below(dirpath):
                  continue
              title = humanize(os.path.basename(dirpath)) if dirpath != root else "Overview"
              stub = (f"---\ntitle: {title}\nexcerpt: ''\nsummary: ''\nhidden: false\n---\n\n"
                      f"# {title}\n")
              open(os.path.join(dirpath, "index.md"), "w", encoding="utf-8").write(stub)
              stubbed += 1
              print(f"  stub index.md -> {os.path.relpath(dirpath, root) or '(root)'}")
          print(f"Stub index.md created in {stubbed} folder(s)")

          # 2) ORDER from SUMMARY.md: parent rel-path ('' = root) -> ordered children.
          entry_re = re.compile(r'^(\s*)[\*\-]\s+\[.*?\]\((.*?)\)')
          children = OrderedDict()

          def register(parent, name):
              children.setdefault(parent, [])
              if name not in children[parent]:
                  children[parent].append(name)

          if os.path.exists(summary_path):
              for line in open(summary_path, encoding="utf-8"):
                  m = entry_re.match(line)
                  if not m:
                      continue
                  rel = m.group(2).strip().lstrip("./")
                  if not rel.endswith(".md"):
                      continue
                  rel = re.sub(r'(^|/)README\.md$', lambda mm: f'{mm.group(1)}index.md', rel)
                  parts = rel.split("/")
                  for i in range(len(parts)):
                      parent = "/".join(parts[:i])
                      name   = parts[i]
                      if name.endswith(".md"):
                          name = name[:-3]
                      register(parent, name)

          # 3) COMPLETENESS: append any on-disk folder/page SUMMARY didn't list, so
          #    nothing is silently dropped from the nav. Walk the tree and register
          #    each subfolder under its parent, and each non-index .md page too.
          for dirpath, dirnames, filenames in os.walk(root):
              if os.path.basename(dirpath) == "assets":
                  dirnames[:] = []
                  continue
              parent_rel = os.path.relpath(dirpath, root).replace(os.sep, "/")
              parent_rel = "" if parent_rel == "." else parent_rel
              for d in sorted(dirnames):
                  if d == "assets":
                      continue
                  if has_markdown_below(os.path.join(dirpath, d)):
                      register(parent_rel, d)
              for f in sorted(filenames):
                  if f.endswith(".md") and f != "index.md":
                      register(parent_rel, f[:-3])

          # 4) WRITE _order.yaml for every folder. index leads, then README/legacy,
          #    then remaining children that actually exist on disk.
          INDEX_NAMES = ["index", "README"]
          written = 0
          skipped_order = 0
          for parent, kids in children.items():
              # Delta: skip folders outside the touched scope (unless full/summary).
              if order_scope is not None and parent not in order_scope:
                  skipped_order += 1
                  continue
              out_dir = os.path.join(root, parent) if parent else root
              if not os.path.isdir(out_dir):
                  continue
              leads = [n for n in INDEX_NAMES
                       if os.path.exists(os.path.join(out_dir, n + ".md"))]
              def exists(c):
                  return (os.path.exists(os.path.join(out_dir, c + ".md"))
                          or os.path.isdir(os.path.join(out_dir, c)))
              ordered = leads + [k for k in kids if k not in leads and exists(k)]
              with open(os.path.join(out_dir, "_order.yaml"), "w", encoding="utf-8") as f:
                  yaml.dump(ordered, f, default_flow_style=False,
                            sort_keys=False, allow_unicode=True)
              written += 1
              print(f"  {parent or '(root)'}/_order.yaml -> {ordered}")

          print(f"_order.yaml written for {written} folder(s); {skipped_order} skipped (delta scope)")
          EOF

      - name: Purge .DS_Store from readme repo
        run: |
          cd readme
          # Remove any .DS_Store (root or nested) from working tree AND git index,
          # so already-committed ones (incl. the repo root) get cleaned on this run.
          find . -path ./.git -prune -o -type f -name '.DS_Store' -print0 \
            | xargs -0 -r git rm -f --quiet --ignore-unmatch

      - name: Commit and push to readme
        run: |
          cd readme
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/Documentation/
          if git diff --staged --quiet; then
            echo "No changes to commit — skipping."
          else
            git commit -m "Sync from docs repo @ ${{ github.sha }}"
            git push origin v1.0
          fi
